

<div class="col-sm-6 col-lg-4">
        <form name="enviar" action="/email/enviar" action="GET">
        <div class="form-group">
            <label for="inputPassword">Selecionar Campaña </label>
        <select class="form-control" name="campana" id="campana">
          <option value="-1">Seleccionar</option> 
           <% campana.forEach(function(el,index){ %>
           <option value="<%=: el.id %>"><%=: el.campana %></option> 
           <% }); %>
      </select>
      </div>
        <div class="form-group">
            <label for="inputPassword">Selecionar Lista </label>
       <select class="form-control" name="idlista">
          <option value="-1">Seleccionar</option> 
           <% listas.forEach(function(el,index){ %>
           <option value="<%=: el.id %>"><%=: el.nombreLista %></option> 
           <% }); %>
      </select>
  		</div>
        <button type="submit" class="btn btn-primary">Enviar</button>
    </form>
    </div>
<div class="col-sm-4 col-lg-6">
     <div class="alert alert-info">
        <a href="#" class="btn btn-xs btn-primary pull-right"></a>
        Preview
    </div>
    <div style="text-align: center !important;">
    <div id="loader" style="display:none;">
      <img src="/images/loader.gif">
      <br><span style="font-size:12px;">Cargando ...</span>
    </div>
    </div>
    <div class="container"  id="verMensaje" style="height: 800px !important;width: 600px !important;overflow: scroll;">
    </div>
</div>